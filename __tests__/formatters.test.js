// import { stylish } from '../src/formaters';

describe('Name of the group', () => {
  describe('Name of the group', () => {
    test('should', () => {
      expect(true).toBe(true);
    });
  });
});
